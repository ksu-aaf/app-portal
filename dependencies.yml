version: 2
dependencies:
- type: php
  path: /
  manifest_updates:
    filters:
    - name: ".*"
      versions: "L.Y.Y"
  settings:
    # options for composer install/update/require
    composer_options: "--ignore-platform-reqs"
 